# Homebrew Tap for dongzhenye's Tools

Personal Homebrew tap for macOS utilities.

## Usage

```bash
brew tap dongzhenye/tap
brew install --cask reverse-scroll-cli
```

## Available Casks

- **reverse-scroll-cli** — Reverse mouse scroll on macOS. Lightweight CLI, no GUI.
